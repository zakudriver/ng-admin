import { VimDirective } from './vim.directive';

describe('VimDirective', () => {
  it('should create an instance', () => {
    const directive = new VimDirective();
    expect(directive).toBeTruthy();
  });
});
